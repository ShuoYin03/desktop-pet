{
  "name": "desktop-pet",
  "packageManager": "npm@10.0.0",
  "private": true,
  "version": "0.1.0",
  "description": "Desktop pet application monorepo",
  "scripts": {
    "lint": "turbo lint",
    "build": "turbo build",
    "dev": "turbo dev"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
